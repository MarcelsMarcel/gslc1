Install Docker, Git
Clone repository dengan command “git clone https://github.com/MarcelsMarcel/gslc1”
Buka folder repository yang sudah diclone dengan VSCode.
Buka Docker Desktop
Buka terminal di VSCode dan jalankan command “docker-compose up -d --build”
Buka port di browser dengan mengetikkan http://localhost:[no. port]
